#pragma once

namespace Bed
{
    void StressTestSystem(ECS& ecs, float deltaTime)
    {
        for(int i = 0; i < ecs.GetAllEntities().size() > i; i++)
        {
            std::cout << deltaTime << "\n";
        }
    }
}